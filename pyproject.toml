[project]
name = "schafkopf-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jakob schleicher", email = "jakob.schleicher@web.de" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
schafkopf-ai = "schafkopf_ai:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.7",
]
